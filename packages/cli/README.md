# context-ray CLI

The CLI package for Context Ray. The root [README](../../README.md) documents commands, report formats, exit codes, and the static/runtime trust boundary.

This package is not published from the local pre-release repository.
